[project]
name = "refund-gate"
version = "0.0.0"
requires-python = ">=3.12"

[project.optional-dependencies]
dev = ["pytest"]
app = ["slack_bolt"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
